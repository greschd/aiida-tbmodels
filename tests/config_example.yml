computers:
    - name: localhost
      hostname: localhost
      description: localhost
      transport: local
      scheduler: direct
      work_directory: /tmp/test_aiida_bandstructure_utils_run

codes:
    - label: tbmodels
      description: tbmodels
      default_plugin: tbmodels.parse
      remote_computer: localhost
      remote_abspath: /home/greschd/.virtualenvs/tbmodels_test/bin/tbmodels
